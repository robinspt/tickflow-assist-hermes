---
name: ta_startmonitor
description: Start TickFlow Assist realtime monitor.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta_startmonitor

Call `start_monitor` with no arguments and return the tool result `text` field.

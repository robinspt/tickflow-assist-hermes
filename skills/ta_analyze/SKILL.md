---
name: ta_analyze
description: Run TickFlow Assist composite analysis for one stock. Args: SYMBOL.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta_analyze

Parse args as `SYMBOL`, call `analyze`, and return the tool result `text` field.

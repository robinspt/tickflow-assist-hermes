---
name: ta_stopdailyupdate
description: Stop TickFlow Assist Hermes cron daily update jobs.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta_stopdailyupdate

Call `stop_daily_update` with no arguments and return the tool result `text` field.

---
name: ta-screenstocks
description: Screen A-share stock candidates with TickFlow Assist. Args: natural language criteria.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta-screenstocks

Pass all args as `keyword` to `screen_stock_candidates` and return the tool result `text` field.

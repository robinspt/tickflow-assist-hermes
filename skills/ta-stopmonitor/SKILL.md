---
name: ta-stopmonitor
description: Stop TickFlow Assist realtime monitor.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta-stopmonitor

Call `stop_monitor` with no arguments and return the tool result `text` field.

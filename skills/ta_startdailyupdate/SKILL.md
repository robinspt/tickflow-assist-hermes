---
name: ta_startdailyupdate
description: Start TickFlow Assist Hermes cron daily update jobs.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta_startdailyupdate

Call `start_daily_update` with no arguments and return the tool result `text` field.

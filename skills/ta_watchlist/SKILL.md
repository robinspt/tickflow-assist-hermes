---
name: ta_watchlist
description: Show TickFlow Assist watchlist.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta_watchlist

Call `list_watchlist` with no arguments and return the tool result `text` field.

---
name: ta-flashstatus
description: Show Jin10 flash monitor status in TickFlow Assist.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta-flashstatus

Call `flash_monitor_status` with no arguments and return the tool result `text` field.

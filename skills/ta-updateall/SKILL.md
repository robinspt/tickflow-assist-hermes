---
name: ta-updateall
description: Update all TickFlow Assist watchlist stocks.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta-updateall

Call `update_all` with no arguments and return the tool result `text` field.

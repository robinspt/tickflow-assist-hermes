---
name: ta-refreshprofiles
description: Refresh watchlist sector/theme profiles. Args: [SYMBOL].
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta-refreshprofiles

Parse optional args as `SYMBOL`, call `refresh_watchlist_profiles`, and return the tool result `text` field.

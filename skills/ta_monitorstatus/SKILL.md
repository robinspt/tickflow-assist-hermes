---
name: ta_monitorstatus
description: Show TickFlow Assist realtime monitor status.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta_monitorstatus

Call `monitor_status` with no arguments and return the tool result `text` field.

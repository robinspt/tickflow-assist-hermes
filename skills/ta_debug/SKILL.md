---
name: ta_debug
description: Show TickFlow Assist debug diagnostics.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta_debug

Use the native `/ta_debug` command when available. If a tool call is needed, summarize that diagnostics are available from `/ta_debug`.

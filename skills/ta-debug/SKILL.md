---
name: ta-debug
description: Show TickFlow Assist debug diagnostics.
metadata:
  hermes:
    plugin: tickflow-assist
---
# /ta-debug

Call `debug_status` with no arguments and return the tool result `text` field.
